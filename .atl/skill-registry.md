# Skill Registry for effectify

> **Auto-generated by sdd-init**\
> This registry catalogs all available skills for sub-agents. Update by re-running `/sdd-init`.

---

## Quick Reference

| Skill           | Trigger                                                  | Location                                           |
| --------------- | -------------------------------------------------------- | -------------------------------------------------- |
| `go-testing`    | Writing Go tests, using teatest, or adding test coverage | `~/.config/opencode/skills/go-testing/SKILL.md`    |
| `skill-creator` | Creating new AI agent skills, adding agent instructions  | `~/.config/opencode/skills/skill-creator/SKILL.md` |

---

## Workflow Skills (SDD)

These skills manage the Spec-Driven Development workflow:

| Skill         | Purpose                                                       |
| ------------- | ------------------------------------------------------------- |
| `sdd-explore` | Explore and investigate ideas before committing to a change   |
| `sdd-propose` | Create a change proposal with intent, scope, and approach     |
| `sdd-spec`    | Write specifications with requirements and scenarios          |
| `sdd-design`  | Create technical design document with architecture decisions  |
| `sdd-tasks`   | Break down a change into an implementation task checklist     |
| `sdd-apply`   | Implement tasks from the change, writing actual code          |
| `sdd-verify`  | Validate that implementation matches specs, design, and tasks |
| `sdd-archive` | Sync delta specs to main specs and archive a completed change |
| `sdd-init`    | Initialize Spec-Driven Development context in a project       |

---

## Project Conventions

### Nx Guidelines (from AGENTS.md)

- **Task Execution**: Always prefer running tasks through `nx` (i.e., `nx run`, `nx run-many`, `nx affected`) instead of using the underlying tooling directly
- **Workspace Analysis**: Use `nx_workspace` tool to gain understanding of workspace architecture
- **Project Details**: Use `nx_project_details` tool to analyze specific project structure and dependencies
- **Nx Docs**: Use `nx_docs` tool for configuration questions and best practices

### CI Error Guidelines

When fixing CI pipeline errors:

1. Retrieve current CIPEs using `nx_cloud_cipe_details`
2. Use `nx_cloud_fix_cipe_failure` to get logs for specific tasks
3. Fix the problem and verify by running the task

---

## Full Skill Details

### go-testing

```yaml
name: go-testing
triggers:
  - writing go tests
  - using teatest
  - adding test coverage
description: >
  Go testing patterns for Gentleman.Dots, including Bubbletea TUI testing.
location: ~/.config/opencode/skills/go-testing/SKILL.md
```

**Critical Patterns**:

- Table-driven tests for multiple test cases
- Bubbletea Model testing via state transitions
- Teatest integration tests for TUI flows
- Golden file testing for visual output

### skill-creator

```yaml
name: skill-creator
triggers:
  - create new skill
  - add agent instructions
  - document patterns for AI
description: >
  Creates new AI agent skills following the Agent Skills spec.
location: ~/.config/opencode/skills/skill-creator/SKILL.md
```

**When to Create**:

- Pattern used repeatedly and AI needs guidance
- Project-specific conventions differ from generic best practices
- Complex workflows need step-by-step instructions

---

## Loading Protocol

**Every sub-agent MUST check this registry FIRST**:

```
1. Try engram: mem_search(query: "skill-registry", project: "effectify")
   → if found: mem_get_observation(id) → full registry
2. If engram not available: read .atl/skill-registry.md
3. From the registry, identify skills matching your task
4. Read those specific SKILL.md files
5. THEN proceed with your actual task
```

---

## Sources Scanned

- `~/.claude/skills/`
- `~/.config/opencode/skills/`
- Project-level: `.agent/skills/` (none found)

_Last updated: 2026-03-14_
