{
  "name": "@effectify/loom-nitro",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/loom/nitro/src",
  "projectType": "library",
  "tags": ["loom", "public", "nitro"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.lib.json && tsc --noEmit -p tsconfig.spec.json",
        "cwd": "packages/loom/nitro"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest run --config vitest.config.mts",
        "cwd": "packages/loom/nitro"
      }
    },
    "lint": {
      "executor": "nx-oxlint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/loom/nitro/**/*.{ts,tsx,js,jsx,mts,cts}"]
      }
    }
  }
}
