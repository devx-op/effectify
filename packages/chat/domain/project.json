{
  "name": "@effectify/chat-domain",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/chat/domain/src",
  "projectType": "library",
  "tags": ["chat"],
  "targets": {
    "lint": {
      "executor": "nx-oxlint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/chat/domain/**/*.{ts,tsx,js,jsx}"]
      }
    }
  }
}
